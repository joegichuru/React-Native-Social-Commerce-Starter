import { createIconSetFromIcoMoon } from 'react-native-vector-icons';
import fontConfig from '../assets/fonts/selection.json';

const Instafont = createIconSetFromIcoMoon(fontConfig);

export default Instafont;
